import React from 'react';
import { View, CheckBox, ScrollView } from 'react-native';
import styles from './styles'
// import { Container } from './styles';

export default function Index() {
  return (
      <ScrollView>
          
      </ScrollView>
  );
}
